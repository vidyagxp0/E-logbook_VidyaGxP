<!DOCTYPE html>
<html>
<head>
  <style>
    body {
      font-family: Arial, sans-serif;
      font-size: 14px;
      margin: 20px;
      line-height: 1.6;
    }

    .report-content {
      margin-top: 30px;
    }

    h1, h2 {
      margin-bottom: 20px;
      text-align: left;
      clear: both;
    }

    h1 {
      font-size: 24px;
    }

    h2 {
      font-size: 18px;
      color: #0066CC;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      margin-bottom: 30px;
      border: 1px solid #000;
    }

    th, td {
      border: 1px solid #000;
      padding: 10px;
      text-align: left;
    }

    th {
      background-color: #f8f8f8;
      font-weight: bold;
    }

    .heading {
      padding-top: 35px;
      text-align: center;
    }

    .remarks {
      page-break-inside: avoid;
    }

    .green {
      color: green;
      font-weight: bold;
    }

    .red {
      color: red;
      font-weight: bold;
    }

    .black {
      color: black;
      font-weight: bold;
    }
  </style>
</head>
<body>
  <div class="report-content">
    <h2 class="heading">HPLC Records</h2>

    <% if (reportData.hplcRecords && reportData.hplcRecords.length > 0) { %>
      <table>
        <thead>
          <tr>
            <th>S.No</th>
            <th>Date</th>
            <th>Sample Name</th>
            <th>Reg No./Lot No.</th>
            <th>Method Used</th>
            <th>Parameter/Activity</th>
            <th>Column No.</th>
            <th>Start Time</th>
            <th>End Time</th>
            <th>No. of Injections</th>
            <th>Done by</th>
            <th>Remark</th>
            <th>Checked By</th>
          </tr>
        </thead>
        <tbody>
          <% reportData.hplcRecords.forEach((record, index) => { %>
            <tr>
              <td><%= index + 1 %></td>
              <td><%= record?.date || "Not Applicable" %></td>
              <td><%= record?.sample_name || "Not Applicable" %></td>
              <td><%= record?.reg_no || "Not Applicable" %></td>
              <td><%= record?.method_used || "Not Applicable" %></td>
              <td><%= record?.parameter_or_activity || "Not Applicable" %></td>
              <td><%= record?.column_no || "Not Applicable" %></td>
              <td><%= record?.start_time || "Not Applicable" %></td>
              <td><%= record?.end_time || "Not Applicable" %></td>
              <td><%= record?.no_of_injections || "Not Applicable" %></td>
              <td><%= record?.done_by || "Not Applicable" %></td>
              <td><%= record?.remarks || "Not Applicable" %></td>
              <td><%= record?.reviewed_by || "Not Applicable" %></td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    <% } else { %>
      <p>No Records Found.</p>
    <% } %>
  </div>
</body>
</html>
